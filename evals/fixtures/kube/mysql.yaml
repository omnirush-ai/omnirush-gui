apiVersion: v1
kind: Secret
metadata:
  name: omnirush-mysql
  labels:
    app.kubernetes.io/name: omnirush-mysql
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: password
  MYSQL_DATABASE: omnirush_den
  MYSQL_USER: omnirush
  MYSQL_PASSWORD: omnirush
---
apiVersion: v1
kind: Service
metadata:
  name: omnirush-mysql
  labels:
    app.kubernetes.io/name: omnirush-mysql
spec:
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
  selector:
    app.kubernetes.io/name: omnirush-mysql
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: omnirush-mysql
  labels:
    app.kubernetes.io/name: omnirush-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: omnirush-mysql
  template:
    metadata:
      labels:
        app.kubernetes.io/name: omnirush-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          imagePullPolicy: IfNotPresent
          ports:
            - name: mysql
              containerPort: 3306
          envFrom:
            - secretRef:
                name: omnirush-mysql
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
          readinessProbe:
            exec:
              command:
                - sh
                - -lc
                - mysqladmin ping -h 127.0.0.1 -uomnirush -p"$MYSQL_PASSWORD"
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 24
      volumes:
        - name: data
          emptyDir: {}

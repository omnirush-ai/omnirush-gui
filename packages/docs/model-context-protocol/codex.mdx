---
title: "OmniRush.ai MCP for Codex"
sidebarTitle: "Codex"
description: "Codex MCP setup for OmniRush.ai Connect, with OAuth access to your org's Gmail, Calendar, Drive, shared connections, skills, and Cloud resources."
---

The OmniRush.ai MCP server ([OmniRush.ai Connect](/cloud/run-in-the-cloud/cloud-mcp))
enables Codex to use the OmniRush.ai capabilities your organization has made
available to you: Gmail read/search, Calendar list/create, Drive search/read,
Gmail draft creation, shared MCP connections such as Notion, Linear, Slack,
Stripe, Exa, and Context7, shared skills, and OmniRush.ai Cloud resources like
skills, plugins, Collections, workers, members, roles, teams, and LLM
providers. The server exposes only `search_capabilities` and
`execute_capability`, so Codex searches what you can access and runs exact
capability names instead of loading hundreds of tool definitions into context.

## Server URL

```text
https://api.omnirush.example.com/mcp/agent
```

omnirush.ai does not run a hosted Den, so there is no public endpoint: use the
`/mcp/agent` URL on the API origin of your [self-hosted Den](/start-here/self-host)
(`api.omnirush.example.com` above is a placeholder). OAuth sign-in uses your
account on that Den, and the organization you pick is pinned into the token.

## Setup

1. Add OmniRush.ai Connect:

   ```sh
   codex mcp add omnirush --url https://api.omnirush.example.com/mcp/agent
   ```

2. Log in and finish the browser sign-in:

   ```sh
   codex mcp login omnirush
   ```

In the Codex desktop app, open `codex://settings/connections` and paste the
server URL there instead.

## Switch organizations

Log out, then log in again and choose the organization you want:

```sh
codex mcp logout omnirush
codex mcp login omnirush
```

<Note>
This is a setup guide. These steps match OmniRush.ai Connect's OAuth profile:
HTTPS or HTTP loopback callbacks, PKCE S256, and dynamic client registration
fallback. Codex is listed as setup only because native proof is not complete.
See the
[client support status](/cloud/run-in-the-cloud/cloud-mcp#client-support-status)
table.
</Note>

## What you can ask

- "Which OmniRush.ai organization am I connected to?"
- "Add omar@example.com to my organization."
- "Search my org's capabilities for calendar tools, then list tomorrow's events."
- "Create a skill that writes weekly status reports and share it with my whole organization."

## What's next

- Read the [hub overview](/cloud/run-in-the-cloud/cloud-mcp) for OAuth, token,
  server exposure, and troubleshooting details.
- Publish Notion, Linear, Slack, and other shared tools with
  [shared MCP connections](/cloud/share-with-your-team/shared-mcp-connections).
- Connect from a remote environment with
  [OmniRush.ai MCP on a remote machine](/cloud/run-in-the-cloud/connect-omnirush-mcp-remote-machine).
